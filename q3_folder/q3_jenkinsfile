pipeline {
    agent any
    stages {
        stage('Parallel Tasks') {
            parallel {
                stage('Fast Task') {
                    steps {
                        bat 'q3_fasttask.bat'
                    }
                }
                stage('Slow Task') {
                    steps {
                        bat 'q3_slowtask.bat'
                    }
                }
            }
        }
    }
}
